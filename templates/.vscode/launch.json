{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "espidf",
          "name": "Launch",
          "request": "launch",
          "env": {
                  "PYTHONPATH": "${command:espIdf.getExtensionPath}/esp_debug_adapter/debug_adapter"
              },
      }
  ]
}
